apiVersion: v1
kind: Service
metadata:
  name: {{ include "mandy-cb-api.fullname" . }}
  labels:
    {{- include "mandy-cb-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mandy-cb-api.selectorLabels" . | nindent 4 }}
