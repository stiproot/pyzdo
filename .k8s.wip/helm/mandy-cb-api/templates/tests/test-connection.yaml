apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mandy-cb-api.fullname" . }}-test-connection"
  labels:
    {{- include "mandy-cb-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mandy-cb-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
